const Dashboard = () => {
    return (
        <div style={{
            display: 'flex',
            justifyContent: 'center',
            alignItems: 'center',
            height: '100vh',
            textAlign: 'center'
        }}>
        <h2>
        Welcome to Dashbord!
        </h2>
        </div>
    )
}

export default Dashboard;


/* Pages

In Next.js, pages are created by adding files to the pages directory. Each file in this directory corresponds to a route in your application.

This will be accessible at / in your application */

